#include "main.h"

/**
 * _abs - computes the absolute vale of an integer
 * @t: the intrger to be computed
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
